backups:
  local:
    connection:
      type:       mysql
      host:       localhost
      username:   cms
      password:   cms.123
    options:
      # database:   mysql
      # server:     true
      encryption:
        password: secret
    upload:
      s3:
        bucket: jpedro-database-dumps
        path:   tests

  dotser:
    connection:
      type:       mysql
      host:       dotser.ie
      username:   dotser
      password:   7dgirqz3wsne
    options:
      master-data: 0
      # database:   mysql
      # server:     true
      encryption:
        password: secret
